<!DOCTYPE html>
<html lang="es">

<head>
    <!-- Include Head -->
    <%- include('partials/head') %>
    <link rel="stylesheet" href="/css/partials.css">
    <link rel="stylesheet" href="/css/register.css">
</head>

<body>
	<!-- Header -->
	<%- include('partials/header') %>
	<!-- /Header -->

	<main>
        <br>
        <br>
        <!------- Formulario ---------->
        <div class="form-container">
        <h2 class="register-title">Formulario de Edicion De Productos</h2>
        <form class="register-form" action="/products/edit/<%= productToEdit.id %>?_method=PUT" method="POST" enctype="multipart/form-data">				
            <div class="form-div">
                <label for="name" class="form-label">Nombre del producto:</label>
                <input type="text" id="name" name="name" value="<%= productToEdit.name %> " class="form-input">
            </div>
            <div class="form-div">
                <label for="bodega" class="form-label">Bodega:</label>
                <input type="bodega" id="bodega" name="bodega" value="<%= productToEdit.bodega %> " class="form-input">
            </div>
            <div class="form-div">
                <label for="varietal" class="form-label">Varietal:</label>
                <input type="text" id="varietal" name="varietal" value="<%= productToEdit.varietal %> " class="form-input">
            </div>
            <div class="form-div">
                <label for="price" class="form-label">Precio del producto:</label>
                <input type="number" id="price" name="price"   value=<%= parseInt(productToEdit.price) %> class="form-input">
            </div>
            <div class="form-div">
                <label for="discount" class="form-label">Descuento:</label>
                <input type="number" id="discount" name="discount" min="0" max="1" step="0.01" value=<%= productToEdit.discount %>  class="form-input">
            </div>
            <div class="form-div">
                <label for="image" class="form-label">Foto:</label>
                <input type="file" id="image" accept=".png, .jpeg, .jpg" name="image" placeholder="Ingrese una foto" class="form-input" value="<%= productToEdit.image[0]%>"  multiple>
            </div>
            <div class="form-div">
                <label for="category" class="form-label">Categoría:</label>
                <select name="category" id="category" class="form-input">
                    <option value="">Elegí</option>
					<option value="Tinto" <%= productToEdit.category == "Tinto" ? "selected" : "" %>>Tinto</option>
					<option value="Blanco" <%= productToEdit.category == "Blanco" ? "selected" : "" %>>Blanco</option>
                    <option value="Rosado" <%= productToEdit.category == "Rosado" ? "selected" : "" %>>Rosado</option>
                    <option value="Espumante" <%= productToEdit.category == "Espumante" ? "selected" : "" %>>Espumante</option>
                </select>
            </div>
            <div class="form-div">
                <label for="description" class="form-label">Descripción:</label>
                <textarea name="description" id="description" class="form-input"><%= productToEdit.description%></textarea>
            </div>
            <div class="form-buttons">
                <button type="submit">Guardar producto</button>
                <button type="reset" class="reset-button">Limpiar</button>
            </div>
    </form>
    <br><br><br>
    </div>
       
        <br>
    </main> 

	<!-- footer -->
	<%- include('partials/footer') %>
	<!-- /footer -->
</body>	
</html>